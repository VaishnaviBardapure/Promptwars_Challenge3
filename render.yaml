services:
  - type: web
    name: PromptWars Carbon Platform
    env: node
    plan: free
    branch: main
    buildCommand: npm install && npm run build
    startCommand: npm start
    autoDeploy: true
